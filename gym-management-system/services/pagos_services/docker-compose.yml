version: '3'
services:
  clients-service:
    build:
      context: .
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8083:8000"
    volumes:
      - .:/app
